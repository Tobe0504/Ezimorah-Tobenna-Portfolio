import React from "react";

const WorkDetail = (props) => {
  return(
      <div>
        {/* {props.works.map(
            (work) => {
                return(
                    <div key = {work.key}>
                        <img src={work.src} alt="GP Calculator" />
                    </div>
                )
            }
        )} */}
      </div>
  )
};


export default WorkDetail;
